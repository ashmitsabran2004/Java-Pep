class prob2{
    byte b1 = 100, b2 = 120; // cant store in byte we have to get the value in integer type otherwise it will throw error
    int res1 = (b1+b2);
    System.out.println(res1);

    short s1 = 1500, s2 = 2500; // cant store in short we have to get the value in integer type otherwise it will throw error
    int res1 = (b1+b2);
    int res2 = (s1 * s2);
    System.out.println(res2);
    char ch1='A', ch2 = 'A'; // cant store in char we have to get the value in integer type otherwise it will throw error
    int res1 = (b1+b2);
    int res3 = ch1 + ch2;
    System.out.println(res3);
}
